/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { DataNascimentoMaskDirective } from './dataNascimento-mask.directive';

describe('Directive: DataNascimentoMask', () => {
  it('should create an instance', () => {
    const directive = new DataNascimentoMaskDirective();
    expect(directive).toBeTruthy();
  });
});
