import { ComponentFixture, TestBed } from '@angular/core/testing';
import { LoginUsuarioForm3Page } from './login-usuario-form3.page';

describe('LoginUsuarioForm3Page', () => {
  let component: LoginUsuarioForm3Page;
  let fixture: ComponentFixture<LoginUsuarioForm3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(LoginUsuarioForm3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
